#!/bin/bash
while true
do
    if [ `pwd` == "/" ]
    then
            echo Cannot find makefile
            exit 1
    fi
    if [ -e Makefile ]
    then
        make check-syntax CHK_SOURCES="$1" QUOTE_INCLUDE_DIRS="$2"
        exit $?
    fi
    cd ..
done
